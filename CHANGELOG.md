# Changelog

## [0.1.0] - 2026-08-25

- Initial release: `search_mockups`, `create_mockups`, `get_renders`, `account_status`.
- Local catalog search over the full ~5300-mockup catalog (the upstream endpoint
  ignores every search parameter).
- Ephemeral upload staging so locally-held images can be rendered without hosting.
